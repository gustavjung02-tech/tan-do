"use client";
import {useEffect,useState} from "react";
type InstallEvent=Event&{prompt?:()=>Promise<void>;userChoice?:Promise<{outcome:string}>};
export function InstallAppButton(){const [event,setEvent]=useState<InstallEvent|null>(null);const [show,setShow]=useState(false);const [ios,setIos]=useState(false);useEffect(()=>{if(!window.matchMedia("(display-mode: standalone)").matches){setShow(true);setIos(/iphone|ipad|ipod/i.test(navigator.userAgent));const h=(e:Event)=>{e.preventDefault();setEvent(e as InstallEvent);setIos(false)};window.addEventListener("beforeinstallprompt",h);return()=>window.removeEventListener("beforeinstallprompt",h)}},[]);if(!show)return null;return <div className="mt-4"><button className="w-full rounded-2xl bg-slate-950 px-4 py-3 text-sm font-black text-white" onClick={async()=>{if(!event?.prompt){setIos(true);return}await event.prompt();const c=await event.userChoice;if(c?.outcome==="accepted")setShow(false)}}>C?i ?ng d?ng T?n ?? F&B</button>{ios&&<p className="mt-2 rounded-xl bg-blue-50 p-3 text-sm font-bold text-blue-900">iPhone/iPad: Safari ? Chia s? ? Th?m v?o M?n h?nh ch?nh.</p>}</div>}
