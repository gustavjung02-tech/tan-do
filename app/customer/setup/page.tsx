"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
export default function CustomerSetupPage(){const r=useRouter();const [f,setF]=useState({name:"",phone:"",address:"",latitude:null as number|null,longitude:null as number|null});async function save(){await fetch('/api/customer/profile',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(f)});r.replace('/customer')}return <main className="phone-page p-5"><section className="mx-auto max-w-md rounded-3xl bg-white p-5"><h1 className="text-xl font-black">Thiết lập quán</h1>{Object.keys(f).slice(0,3).map(k=><input key={k} className="mt-3 w-full rounded-xl border p-3" placeholder={k} value={(f as any)[k]} onChange={e=>setF({...f,[k]:e.target.value})}/>)}<button onClick={()=>navigator.geolocation.getCurrentPosition(p=>setF({...f,latitude:p.coords.latitude,longitude:p.coords.longitude}))} className="mt-3 w-full rounded-xl bg-slate-100 p-3">📍 Lấy vị trí</button><button onClick={save} className="mt-3 w-full rounded-xl bg-emerald-700 p-3 text-white">Lưu</button></section></main>}

