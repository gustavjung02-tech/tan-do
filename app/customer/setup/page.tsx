"use client";
import {useState} from "react";
import {useRouter} from "next/navigation";
export default function CustomerSetupPage(){const router=useRouter();const [form,setForm]=useState({name:"",phone:"",contact_name:"",address:"",area:"",ward:"",district:"",province:"",note:"",latitude:null as number|null,longitude:null as number|null});function set(k:string,v:any){setForm(x=>({...x,[k]:v}))}async function save(){await fetch("/api/customer/profile",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(form)});router.replace("/customer")}return <main className="phone-page p-4"><section className="mx-auto max-w-md rounded-3xl bg-white p-5"><h1 className="text-xl font-black">Thi?t l?p th?ng tin qu?n</h1>{Object.keys(form).filter(k=>!k.includes("latitude")&&!k.includes("longitude")).map(k=><input key={k} className="mt-3 w-full rounded-xl border p-3" placeholder={k} value={(form as any)[k]} onChange={e=>set(k,e.target.value)}/>)}<button className="mt-3 w-full rounded-xl bg-slate-100 p-3" onClick={()=>navigator.geolocation.getCurrentPosition(p=>{set("latitude",p.coords.latitude);set("longitude",p.coords.longitude)})}>L?y v? tr? hi?n t?i</button><button className="mt-3 w-full rounded-xl bg-emerald-700 p-3 font-black text-white" onClick={save}>L?u th?ng tin</button></section></main>}
